# ベンダーマスターAPI連携要件

## 1. 目的

対象業務機能において、ベンダー情報を手入力するのではなく、既存のベンダーマスター情報をAPI経由で検索・取得し、候補から選択できるようにする。

ベンダーマスターは既存の連携基盤と同期されているため、本システムからは連携基盤上のベンダー情報を参照する想定とする。

## 2. システム構成

```text
ベンダーマスターシステム
  ↓ 既存連携
連携基盤
  ↓ API連携
本システムのバックエンドAPI
  ↓
対象業務画面
```

フロントエンドから連携基盤を直接呼び出さず、本システムのバックエンドAPIを経由する。

## 3. 利用概要

- 対象機能：ベンダー選択を伴う業務データの登録・編集
- 利用者：業務ユーザー
- 利用目的：ベンダー検索および選択
- データ方向：連携基盤から本システムへの参照のみ
- 更新・登録：本システムから連携基盤への更新は行わない
- 正データ：ベンダーマスターシステムまたは連携基盤上のベンダーマスター
- 呼び出し契機：ユーザーがベンダー名またはベンダーコードを入力して検索したとき

## 4. 想定する画面動作

1. ユーザーが対象業務データの登録・編集画面を開く。
2. ベンダー名またはベンダーコードを入力する。
3. 本システムのバックエンドAPIが連携基盤のAPIを呼び出す。
4. 条件に一致するベンダー候補を一覧表示する。
5. ユーザーが対象ベンダーを選択する。
6. 選択したベンダー識別子、コード、名称を対象業務データに保存する。

## 5. 取得候補項目

最低限、以下の項目を取得候補とする。

| 項目 | 用途 |
|---|---|
| ベンダーID | 連携基盤またはベンダーマスターシステム上の一意識別子 |
| ベンダーコード | 業務上の識別・検索 |
| ベンダー正式名称 | 画面表示・帳票表示 |
| ベンダー略称 | 必要に応じた画面表示 |
| 有効・無効 | 利用可能なベンダーの判定 |
| 取引可能状態 | 取引停止ベンダー等の除外 |
| 国・地域 | 必要に応じた絞り込み |
| ベンダー区分 | 必要に応じた絞り込み |
| 最終更新日時 | キャッシュ・同期状態の判断 |

実際の取得項目は、ベンダーマスターシステムから連携基盤へ連携されている項目と業務要件を確認して決定する。

## 6. 本システム側の保存方針

基本的には連携基盤側を正とし、本システム内にベンダーマスターを複製しない。

対象業務データには、選択時点の以下の情報を保存する想定とする。

```text
vendorId
vendorCode
vendorName
```

名称変更後も過去データの表示を維持する必要がある場合は、ベンダーIDだけでなく選択時点の名称も保持する。

## 7. API利用方式

### 7.1 推奨構成

```text
ブラウザ
  ↓
本システムのバックエンドAPI
  ↓
連携基盤API
```

### 7.2 バックエンド経由とする理由

- 連携基盤の認証情報をブラウザへ公開しないため
- 認証・認可を一元管理するため
- API呼び出しログと監査ログを記録するため
- タイムアウト、再試行、キャッシュを制御するため
- 連携基盤側のAPI仕様変更を本システム内で吸収するため

## 8. 認証・認可要件

### 8.1 希望する認証方式

システム間連携用のOAuth 2.0を第一候補とする。

個人ユーザーのアカウントは使用せず、API連携専用のクライアントまたはサービスアカウントを使用する。

### 8.2 権限

- ベンダー情報に対する読み取り専用権限
- 対象テーブルまたは対象APIのみに限定
- 登録・更新・削除権限は付与しない
- 開発環境と本番環境で認証情報を分離

### 8.3 認証情報の管理

- クライアントシークレット等はAzure Key Vaultで管理する
- ソースコード、設定ファイル、フロントエンドには保存しない
- 定期的なシークレット更新方法を決める

### 8.4 代替方式

OAuth 2.0を利用できない場合は、組織標準のAPI Gatewayまたは専用サービスアカウントによる方式を確認する。

Basic認証を利用する場合も、個人アカウントは使用せず、読み取り専用の専用サービスアカウントとする。

## 9. 非機能要件

### 9.1 性能

- ベンダー検索結果は必要件数のみ返す
- 1回の検索結果は10～50件程度を想定
- 全件取得は原則行わない
- 部分一致検索、前方一致検索、ページングの可否を確認する

### 9.2 可用性・障害対応

連携基盤APIが利用できない場合、対象業務機能全体を利用不能にしない設計を検討する。

候補となる対応は以下とする。

- 画面に「ベンダー情報を取得できません」と表示する
- 一定回数のみ再試行する
- 短時間のキャッシュを利用する
- API障害をApplication Insights等へ記録する
- 手入力を許可するかは業務側と別途判断する

### 9.3 ログ・監査

以下を記録する。

- API呼び出し日時
- 呼び出し元機能
- 成功・失敗
- HTTPステータス
- 処理時間
- エラー内容
- 相関ID

認証トークンやシークレット、不要な個人情報はログへ出力しない。

## 10. 打ち合わせで説明する内容

以下の内容を連携基盤またはベンダーマスターシステムの担当者へ説明する。

> 本システムでは、業務データを登録する際にベンダーを選択する必要があります。既存のベンダーマスターから連携基盤へ同期されている最新のベンダー情報を、API経由で検索・参照したいと考えています。本システムからベンダー情報を登録・更新する予定はなく、参照のみです。フロントエンドから直接呼び出さず、Azure上のバックエンドAPIからシステム間連携として呼び出す想定です。

## 11. 打ち合わせで確認する事項

### 11.1 API・データ

- ベンダー情報を取得できる既存APIはあるか
- 標準APIを使用できるか
- 専用REST APIが必要か
- 対象となるデータソースまたはテーブルは何か
- ベンダーマスターから連携基盤へ連携されている項目は何か
- ベンダーIDとして利用すべき項目は何か
- 有効、無効、取引停止、削除済みを判別できるか
- API仕様書またはOpenAPI定義はあるか
- サンプルリクエスト、サンプルレスポンスはあるか

### 11.2 検索条件

- ベンダーコードで完全一致検索できるか
- ベンダー名で部分一致または前方一致検索できるか
- 有効なベンダーだけに絞り込めるか
- 国、地域、ベンダー区分で絞り込めるか
- 検索文字数の最低条件はあるか
- ページングに対応しているか
- 1回当たりの最大取得件数はいくつか
- 検索結果の並び順を指定できるか

### 11.3 認証・認可

- 利用可能な認証方式は何か
- OAuth 2.0によるシステム間認証を利用できるか
- OAuth 2.0の場合、利用可能なグラント方式は何か
- API連携専用クライアントまたはサービスアカウントを発行できるか
- 読み取り専用権限に制限できるか
- クライアントID、シークレット、証明書は誰が発行・管理するか
- アクセストークンの有効期限と更新方法は何か
- シークレットや証明書の更新周期は何か
- IPアドレス制限や接続元制限はあるか

### 11.4 環境・ネットワーク

- 開発・検証環境はあるか
- テスト用ベンダーデータは用意されているか
- 開発環境と本番環境でエンドポイントを分離できるか
- Azure App Serviceから接続できるか
- インターネット経由かプライベートネットワーク経由か
- 固定送信元IPの登録が必要か
- プロキシまたはAPI Gatewayを経由する必要があるか
- TLS、クライアント証明書等の要件はあるか

### 11.5 制限・性能

- APIのレート制限はあるか
- 利用可能な時間帯に制限はあるか
- タイムアウトの基準はあるか
- 推奨する検索頻度はあるか
- 同時接続数の制限はあるか
- API応答時間の目安はどの程度か

### 11.6 運用・保守

- API提供・運用の担当部署はどこか
- 利用申請や審査が必要か
- API利用開始までに必要な手続きは何か
- 障害時の問い合わせ先はどこか
- 連携基盤メンテナンス時の通知方法は何か
- データ構造やAPI仕様変更時の通知方法は何か
- 項目変更に対する互換性はどのように管理されるか
- API利用ログまたは監査ログを確認できるか
- SLAまたは可用性の取り決めはあるか

## 12. 打ち合わせで必ず確定したい事項

1. 利用するAPIまたはデータソース
2. 取得可能な項目
3. 利用可能な検索条件
4. 認証方式と権限
5. 開発・本番の接続先
6. ネットワークおよび接続元制限
7. API利用申請と担当部署
8. 障害・変更時の運用窓口

## 13. 現時点の推奨方針

- 既存の連携基盤をベンダー情報の参照元とする
- 本システムからは読み取り専用で利用する
- バックエンドAPIから連携基盤APIを呼び出す
- OAuth 2.0によるシステム間認証を第一候補とする
- 認証情報はAzure Key Vaultで管理する
- ベンダーマスター全件を本システムへ複製しない
- 画面検索時に必要な件数のみ取得する
- 連携基盤障害時の画面動作と代替運用を事前に決める